import React from 'react';
import ReactDOM from 'react-dom';
import './styles/scanf.css'; // Importing the global CSS file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
